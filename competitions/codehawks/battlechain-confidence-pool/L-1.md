# [L-01] `sweepUnclaimedBonus` re-snapshots a drained `totalBonus` on moderator re-flag, permanently short-changing the whitehat bounty

**Contest:** BattleChain Confidence Pools - Cyfrin CodeHawks  
**Submission:** [#118](https://codehawks.cyfrin.io/c/2026-07-battlechain-confidence-pools/s/118)  
**Severity:** Low (Impact: Medium | Likelihood: Low)  
**Status:** ✅ Validated


## Summary

`sweepUnclaimedBonus` zeros out `totalBonus` while deliberately leaving `claimsStarted = false` so the moderator's re-flag window stays open. If the moderator then corrects an erroneous `SURVIVED` flag to `CORRUPTED`, `flagOutcome` re-snapshots the live `totalBonus` which is now zero, so `bountyEntitlement` is computed as `snapshotTotalStaked + 0` instead of `snapshotTotalStaked + B`. The bonus `B` is permanently transferred to the sponsor's `recoveryAddress` with no on-chain recovery path.


## Root Cause

`flagOutcome` unconditionally re-snapshots `totalBonus` on every call, including re-flags:

```solidity
// ConfidencePool.sol::flagOutcome (on re-flag)
// re-snapshots live totalBonus, which is now 0 after the sweep
snapshotTotalBonus = totalBonus; // reads 0 instead of B
bountyEntitlement = willBeGoodFaithCorrupted
    ? snapshotTotalStaked + snapshotTotalBonus // S + 0 instead of S + B
    : 0;
```

`sweepUnclaimedBonus` zeros `totalBonus` but does not set `claimsStarted`, keeping the re-flag window open for this exact scenario:

```solidity
// ConfidencePool.sol::sweepUnclaimedBonus
// @> totalBonus zeroed out here while claimsStarted stays false
if (totalEligibleStake == 0 || riskWindowStart == 0) {
    totalBonus -= amount <= totalBonus ? amount : totalBonus; // totalBonus = 0
}
// @> claimsStarted intentionally NOT set, re-flag window remains open
```


## Impact

The named whitehat receives only `snapshotTotalStaked` instead of the intended `snapshotTotalStaked + snapshotTotalBonus`. Bonus `B` is irrecoverably transferred to the sponsor's `recoveryAddress`.

In a stakerless pool the effect is total: `bountyEntitlement` computes to zero and `claimAttackerBounty` reverts outright. The pool stays solvent, but the entire bounty incentive is destroyed.


## Likelihood

Four conditions must coincide in the same pool lifecycle:

1. No pool interaction during `UNDER_ATTACK` (or there are no stakers), leaving `riskWindowStart == 0`.
2. Moderator flags `SURVIVED` on a genuinely `CORRUPTED` registry.
3. Moderator corrects to good-faith `CORRUPTED` with a named attacker.
4. Sponsor calls `sweepUnclaimedBonus` between the two flags.

Steps 1–3 are individually unlikely. Step 4 is near-certain once the rest holds: the sweep is permissionless and the sponsor receives `B` directly, giving them clear financial incentive to act the moment an erroneous flag lands.


## Attack Scenario

> Alice and Bob stake 150 tokens total. Carol contributes a 40-token bonus. The registry reaches `CORRUPTED` with `riskWindowStart == 0` because no interaction occurred during `UNDER_ATTACK`.

1. Moderator calls `flagOutcome(SURVIVED, false, address(0))` by mistake.  
   → `snapshotTotalBonus = 40`, `claimsStarted = false`.
2. Sponsor calls `sweepUnclaimedBonus`.  
   → `token.balanceOf(recovery) += 40`, `totalBonus = 0`, `claimsStarted` still `false`.
3. Moderator corrects: `flagOutcome(CORRUPTED, true, attacker)`.  
   → `snapshotTotalBonus = totalBonus = 0`, `bountyEntitlement = 150`.
4. Attacker calls `claimAttackerBounty`.  
   → Receives 150, not 190. Sponsor keeps the 40.


## Proof of Concept

```solidity
// SPDX-License-Identifier: MIT
pragma solidity 0.8.26;

import {IAttackRegistry} from "@battlechain/interface/IAttackRegistry.sol";
import {PoolStates} from "src/libraries/PoolStates.sol";
import {BaseConfidencePoolTest} from "test/helpers/BaseConfidencePoolTest.sol";

// forge test --match-test testPoc_SweepBonusBeforeReflagShortchangesAttacker -vvvv
contract SweepBonusCorruptedReflagPoC is BaseConfidencePoolTest {
    function testPoc_SweepBonusBeforeReflagShortchangesAttacker() external {
        uint256 S = 150 * ONE;
        uint256 B = 40  * ONE;

        _stake(alice, 100 * ONE);
        _stake(bob,    50 * ONE);
        _contributeBonus(carol, B);

        attackRegistry.setAgreementState(IAttackRegistry.ContractState.CORRUPTED);
        assertEq(pool.riskWindowStart(), 0);

        // Step 1: wrong flag
        vm.prank(moderator);
        pool.flagOutcome(PoolStates.Outcome.SURVIVED, false, address(0));
        assertEq(pool.snapshotTotalBonus(), B);
        assertEq(pool.claimsStarted(), false);

        // Step 2: sponsor sweeps
        uint256 recoveryBefore = token.balanceOf(recovery);
        pool.sweepUnclaimedBonus();
        assertEq(token.balanceOf(recovery) - recoveryBefore, B);
        assertEq(pool.totalBonus(),    0);
        assertEq(pool.claimsStarted(), false);

        // Step 3: moderator corrects
        vm.prank(moderator);
        pool.flagOutcome(PoolStates.Outcome.CORRUPTED, true, attacker);
        assertEq(pool.snapshotTotalBonus(), 0); // should be B
        assertEq(pool.bountyEntitlement(), S);   // should be S + B

        // Step 4: attacker claims
        vm.prank(attacker);
        pool.claimAttackerBounty();

        assertEq(token.balanceOf(attacker),               S); // missing B
        assertEq(token.balanceOf(recovery) - recoveryBefore, B); // sponsor kept B
        assertEq(token.balanceOf(address(pool)),          0);
    }
}
```


## Recommended Mitigation

On a re-flag, preserve the existing `snapshotTotalBonus` rather than re-reading `totalBonus`. Stakes can legitimately change between flags; the bonus cannot be legitimately depleted between flags - only a sweep that bypasses `claimsStarted` can do so.

```diff
 function flagOutcome(
     PoolStates.Outcome newOutcome,
     bool goodFaith_,
     address attacker_
 ) external onlyModerator {
     if (outcome != PoolStates.Outcome.UNRESOLVED && claimsStarted) revert OutcomeAlreadySet();

+    bool isReflag = outcome != PoolStates.Outcome.UNRESOLVED;
     IAttackRegistry.ContractState state = _observePoolState();

     // ... existing validation ...

     outcome          = newOutcome;
     goodFaith        = goodFaith_;
     attacker         = attacker_;
     snapshotTotalStaked    = totalEligibleStake;
-    snapshotTotalBonus     = totalBonus;
+    snapshotTotalBonus     = isReflag ? snapshotTotalBonus : totalBonus;
     snapshotSumStakeTime   = sumStakeTime;
     snapshotSumStakeTimeSq = sumStakeTimeSq;
```


> *"Impact - Medium: Up to the entire bonus pool can be routed to the wrong party for good, with no on-chain way to unwind it ... The pool stays solvent throughout with no principal ever at risk, and the money ends up at the sponsor's own recoveryAddress."*  
>
> *"Likelihood - Low: Four separate things have to coincide ... assembling the first three in one pool lifecycle is where this stays rare."*

SFinal severity: **Low (Medium impact / Low likelihood)**
